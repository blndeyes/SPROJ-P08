{
  "version": 2,
  "builds": [{ "src": "package.json", "use": "@vercel/static-build", "config": { "distDir": "build" } }],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(manifest\\.json|favicon\\.ico|logo.*\\.png)$", "dest": "/$1" },
    { "src": "/(.*)", "dest": "/" }
  ]
}
